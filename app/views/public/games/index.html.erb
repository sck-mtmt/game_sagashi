<div class="container">
  <div class="row">
    <div class=title>
      <h3>ゲーム一覧</h3>
    </div>
  </div>
    <p>レビューを投稿するにはゲーム選択後<strong>プレイしたいゲームに登録</strong>してからできます</p>

  <%= form_tag(public_games_search_path, method: :get) do %>
    <%= f.text_field_tag :game_title, @title %>
    <%= button_tag type: "submit" %>
  <% end %>



    <table class="table">
      <thead>
        <tr>
          <th></th>
          <th>ゲーム名</th>
          <th>機種</th>
          <th>価格</th>
        </tr>
      </thead>
      <tbody>
       <% if @games %>
        <% @games.each do |game| %>
        <tr>
          <td></td>
          <td><%= link_to public_game_path(game.id) do %>
              <%= game.game_title %>
              <% end %></td>
          <td><%= game.game_model %></td>
          <td><%= game.price %>円</td>
        <% end %>
       <% end %>
        </tr>
      </tbody>
     </table>
</div>




#検索結果を表示
<% if @games %>
  <% @books.each do |book| %>
  #ご自身が表示させたいデータを記載してください。
  #以下のコードではは画像、タイトル、著者名、商品の説明を表示させています。
    <%= image_tag book.image_url %>
    <%= book.title %>
    <%= book.author %>
    <%= book.item_caption %>
  <% end %>
<% end %>



